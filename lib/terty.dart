import 'package:flutter/material.dart';

class terty extends StatelessWidget {
  const terty({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(children: [
        Row(
          children: [
            Text("JNVJNKMVK"),
          ],
        )
      ]),
    );
  }
}
