/*import 'package:flutter/material.dart';

class Step33Details extends StatelessWidget {
  final String braithDate;
  final String gender;
  final String province;
  final String address;

  const Step33Details({
    Key? key,
    required this.braithDate,
    required this.gender,
    required this.province,
    required this.address,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('تفاصيل الصفحة'),
      ),
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text('تاريخ الميلاد: $braithDate'),
            Text('الجنس: $gender'),
            Text('المحافظة: $province'),
            Text('العنوان: $address'),
          ],
        ),
      ),
    );
  }
}
*/